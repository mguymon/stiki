<div class="row-fluid">
  <div class="span8">
    <div class="row-fluid well">
      <h2>Edit <%= @page.title %></h2>
    </div>
          
    <div class="row-fluid">
      <%= form_tag( stiki.space_page_path(@space, @page), :class => 'form-horizontal', :method => :put) do |f| %>
        <%= render( :partial => 'stiki/pages/form', :locals => { :f => f, :page => @page } ) %>
      <% end -%>
    </div>
  </div>
</div>